apiVersion: 1

providers:
  - name: StackSift
    orgId: 1
    folder: StackSift
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
