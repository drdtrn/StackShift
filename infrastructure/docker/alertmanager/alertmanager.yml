global:
  resolve_timeout: 5m

route:
  receiver: webhook
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: webhook
    webhook_configs:
      - url: http://host.docker.internal:9099/alertmanager
        send_resolved: true
